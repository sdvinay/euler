// doesn't work yet
// Find the largest palindrome made from the product of two 3-digit numbers

val start:Int = 100
val end:Int = 999

var max:Int = 0
for (a <- (start to end))
	for (b <- (a to end)) {
		var prdct: Int = a*b
		if (
	}
		
